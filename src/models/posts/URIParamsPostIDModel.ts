/**
 * Represents the URI parameters for a blog post request.
 */
export type URIParamsPostIDModel = {
    /**
     * ID of the requested blog post, provided as a string.
     * @type {string}
     * @required
     */
    id: string;
};
