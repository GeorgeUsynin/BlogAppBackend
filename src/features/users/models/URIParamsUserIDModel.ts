/**
 * Represents the URI parameters for a user request.
 */
export type URIParamsUserIDModel = {
    /**
     * ID of the requested user, provided as a string.
     * @type {string}
     * @required
     */
    id: string;
};
