/**
 * Represents the URI parameters for a comment request.
 */
export type URIParamsCommentIDModel = {
    /**
     * ID of the requested comment, provided as a string.
     * @type {string}
     * @required
     */
    id: string;
};
