/**
 * Represents the URI parameters for a device session request.
 */
export type URIParamsDeviceIDModel = {
    /**
     * ID of the device session to be terminated.
     * Provided as a string in the request path.
     * @type {string}
     * @required
     */
    id: string;
};
